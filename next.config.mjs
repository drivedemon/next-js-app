/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {},
    reactStrictMode: true,
    swcMinify: true,
    output: 'standalone',
    distDir: ".next",
}

export default nextConfig
